# Copy to `.env` (gitignored) and fill in.
# Used by client scripts / curl examples; wrangler itself reads `.dev.vars`.

API_KEY=replace-with-the-key-you-set-via-wrangler-secret
WORKER_URL=https://your-subdomain.workers.dev
